name: List Endpoints
on:
  pull_request:
    branches:
      - main

jobs:
  list-endpoints:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Wait for Vercel Preview Deployment
        uses: patrickedqvist/wait-for-vercel-preview@d7982701e6fcd3ae073bff929e408e004404d38d # v1.3.3
        id: preview
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          allow_inactive: true
        continue-on-error: true

      - name: List Endpoints
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        env:
          PREVIEW_URL: ${{ steps.preview.outputs.url }}
        with:
          script: |
            const previewUrl = process.env.PREVIEW_URL;

            // URLの検証
            if (previewUrl && !/^https:\/\/[a-zA-Z0-9.-]+\.vercel\.app/.test(previewUrl)) {
              core.warning('Invalid Vercel preview URL');
              return;
            }

            const endpoints = [
              '/api/fetch_syllabus',
            ];

            let commentBody = '## 🚀 Deployed Endpoints\n\n';
            endpoints.forEach(endpoint => {
              commentBody += `- [${endpoint}](${previewUrl}${endpoint})\n`;
            });

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: commentBody,
            });
