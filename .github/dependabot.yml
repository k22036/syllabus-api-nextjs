version: 2
updates:
  - package-ecosystem: "bun"
    directory: "/"
    schedule:
      interval: "weekly"
    allow:
      - dependency-type: "all"
    cooldown:
      default-days: 7
      semver-major-days: 5
      semver-minor-days: 3
      semver-patch-days: 1
